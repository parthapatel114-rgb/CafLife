# Optional: the app runs in sample-preview mode without these.
# Use the Supabase project URL and publishable (or legacy anon) key.
# Never use a service-role or secret key: this config is sent to the browser.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
